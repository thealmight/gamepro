# Render Environment Variables for Econ Empire

# JWT Secret - Set this to a strong random string in Render dashboard
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Database Configuration - Will be set automatically by Render
# DATABASE_URL will be provided by Render when using their PostgreSQL service

# Server Configuration
PORT=4000
NODE_ENV=production

# Frontend URL - Set this to your Render frontend URL in Render dashboard
FRONTEND_URL=https://econ-empire-frontend.onrender.com

# Logging
LOG_LEVEL=info